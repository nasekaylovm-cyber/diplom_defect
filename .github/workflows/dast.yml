name: DAST

on: 
  workflow_dispatch:

jobs:
  dast:
    runs-on: self-hosted
    
    steps:
    - name: ZAP Scan
      run: |
        docker run --rm --network host \
          owasp/zap2docker-weekly zap-baseline.py \
          -t http://localhost:8080
